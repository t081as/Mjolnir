stages:
    - test
    - deploy
    - documentation

release:
    stage: deploy
    only:
        - tags
    artifacts:
        name: "${CI_PROJECT_NAME}-${CI_COMMIT_TAG}"
        paths:
          - ./*.nupkg
    script:
        - ./build.ps1 --configuration Release --buildnumber $CI_PIPELINE_IID
        - dotnet nuget push Mjolnir*.nupkg -k $NUGET_API_KEY -s https://api.nuget.org/v3/index.json --no-symbols 1

debug:
    stage: test
    script:
        - ./build.ps1

pages:
  stage: documentation
  only:
  - tags
  artifacts:
    paths:
    - public
  script:
  - ./build.ps1 --configuration Release --buildnumber $CI_PIPELINE_IID